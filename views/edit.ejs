<!DOCTYPE html>
<html>
<head>
    <title>ArtifyMe - Edit Commission</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <nav class="navbar bg-dark border-bottom border-body" data-bs-theme="dark">
        <div class="nav-brand">ArtifyMe</div>
        <div class="nav-links">
            <a href="/">Home</a>
            <a href="/catalog">Catalog</a>
            <span class="user-welcome">Welcome, <%= user.username %></span>
            <a href="/auth/logout">Logout</a>
        </div>
    </nav>
    <div class="dashboard-container">
        <div class="edit-commission">
            <h2>Edit Commission</h2>
            <form action="/customer/commission/update/<%= commission.id %>" method="POST">
                <div class="form-group">
                    <label>Customer Name:</label>
                    <input type="text" name="customer_name" value="<%= commission.customer_name %>" required>
                </div>
                <div class="form-group">
                    <label>Art Type:</label>
                    <select name="art_type" required>
                        <option value="digital" <%= commission.art_type === 'digital' ? 'selected' : '' %>>Digital Art</option>
                        <option value="traditional" <%= commission.art_type === 'traditional' ? 'selected' : '' %>>Traditional Art</option>
                        <option value="sketch" <%= commission.art_type === 'sketch' ? 'selected' : '' %>>Sketch</option>
                    </select>
                </div>
                <div class="form-group">
                    <label>Description:</label>
                    <textarea name="description" required><%= commission.description %></textarea>
                </div>
                <div class="form-group">
                    <label>Price:</label>
                    <input type="number" name="price" value="<%= commission.price %>" required>
                </div>
                <div class="form-group">
                    <label>Status:</label>
                    <select name="status" required>
                        <option value="pending" <%= commission.status === 'pending' ? 'selected' : '' %>>Pending</option>
                        <option value="in-progress" <%= commission.status === 'in-progress' ? 'selected' : '' %>>In Progress</option>
                        <option value="completed" <%= commission.status === 'completed' ? 'selected' : '' %>>Completed</option>
                        <option value="cancelled" <%= commission.status === 'cancelled' ? 'selected' : '' %>>Cancelled</option>
                    </select>
                </div>
                <div class="form-buttons">
                    <button type="submit" class="btn-primary">Update Commission</button>
                    <a href="/customer/dashboard" class="btn-secondary">Cancel</a>
                </div>
            </form>
        </div>
    </div>
</body>
</html>